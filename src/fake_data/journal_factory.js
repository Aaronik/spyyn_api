module.exports = function journalFactory (id) {
  return {
    link: 'http://awesomejournalnumber' + id + '.com',
    id: id
  };
};

// what metadata exists in here? 
// -Typical 'skew', tendency towards some party
